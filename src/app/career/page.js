import CareerContent from "./CareerContent";

export const metadata = {
  title: "Why Join Our Team | YOUTAX India",
  description:
    "Shaping financial experts for a stronger tomorrow — join YOUTAX India and grow with mentors, tools, and real client exposure.",
};

export default function CareerPage() {
  return <CareerContent />;
}
